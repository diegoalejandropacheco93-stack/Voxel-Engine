name: Build

on:
  push:
    branches: [ main, experimental ]
  pull_request:
    branches: [ main, experimental ]
  workflow_dispatch:

jobs:
  buildWindows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4.1.7

      - uses: krdlab/setup-haxe@master
        with:
          haxe-version: 4.3.4
      
      - name: Install Haxelib
        run: |
          haxelib setup C:/haxelib
          haxelib install hxcpp > /dev/null --quiet
          haxelib install lime > /dev/null --quiet
          haxelib install openfl > /dev/null --quiet
          haxelib install flixel > /dev/null --quiet
          haxelib install flixel-tools > /dev/null --quiet
          haxelib install flixel-ui > /dev/null --quiet
          haxelib install flixel-addons > /dev/null --quiet
          haxelib install hscript > /dev/null --quiet
          haxelib install hscript-iris > /dev/null --quiet
          haxelib install tjson > /dev/null --quiet
          haxelib install hxDiscordRpc > /dev/null --quiet
          haxelib install flixel-addons 3.2.2 > /dev/null --quiet
          haxelib run lime setup flixel
          haxelib run lime setup
        shell: cmd
        
      - name: Skip SScript setup mode
        run: echo 'oy9:showMacroty8:loopCosti25y10:includeAllfg' >> %USERPROFILE%/settings.cocoa
        shell: cmd
        
      - name: Create Version Tag
        run: echo "${{github.run_id}}" > VERSION
        
      - name: Compile
        run: haxelib run lime build windows --app-version="4.0.0-${{ github.run_id}}" -D officialBuild
        
      - name: Publish Artifact
        uses: actions/upload-artifact@v4.3.4
        with:
          name: windowsBuild
          path: export/release/windows/bin
